"$schema" = "schemas/schema.json"

[default]
description = "Default permissions for the codedeck-stt plugin: fire the system speech recognizer and read its result"
permissions = ["allow-recognize-speech"]
