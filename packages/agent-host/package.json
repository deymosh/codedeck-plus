{
  "name": "@codedeck/agent-host",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CodeDeck+ agent host — the Node sidecar the bridge runs agent SDKs in (Claude Code, OpenCode), spoken to over the driver protocol on stdio.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gen:platform-packages": "vitest run -u src/__tests__/lockfilePins.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.280",
    "@opencode-ai/sdk": "^1.18.32",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "opencode-ai": "1.18.32",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "license": "MIT"
}
