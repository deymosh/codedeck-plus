// GENERATED by `cargo test -p agent-protocol --test gen_ts_bindings -- --ignored` — do not hand-edit.
// Source of truth: crates/agent-protocol (and the crates/protocol types it references).

// This file has been generated by Specta. Do not edit this file manually.
/**
 *  An agent the host can run, as its driver describes it. The bridge turns
 *  this into the phone-facing `AgentDescriptor` by adding credential status.
 */
export type AgentInfo = AgentInfo_Serialize | AgentInfo_Deserialize;

/**
 *  An agent the host can run, as its driver describes it. The bridge turns
 *  this into the phone-facing `AgentDescriptor` by adding credential status.
 */
export type AgentInfo_Deserialize = {
	id: string,
	displayName: string,
	modes?: OptionChoice_Deserialize[],
	efforts?: OptionChoice_Deserialize[],
	defaultMode?: string | null,
	supports?: AgentSupports,
	credentials?: CredentialSpec_Deserialize[],
	/**
	 *  Set when the driver is installed but cannot run sessions here (e.g. a
	 *  missing binary); the bridge refuses sessions with this reason.
	 */
	unavailableReason?: string | null,
};

/**
 *  An agent the host can run, as its driver describes it. The bridge turns
 *  this into the phone-facing `AgentDescriptor` by adding credential status.
 */
export type AgentInfo_Serialize = {
	id: string,
	displayName: string,
	modes: OptionChoice_Serialize[],
	efforts: OptionChoice_Serialize[],
	defaultMode?: string | null,
	supports: AgentSupports,
	credentials: CredentialSpec_Serialize[],
	/**
	 *  Set when the driver is installed but cannot run sessions here (e.g. a
	 *  missing binary); the bridge refuses sessions with this reason.
	 */
	unavailableReason?: string | null,
};

/**
 *  Optional features an agent supports. A client offers a feature only when
 *  the session's agent advertises it.
 */
export type AgentSupports = {
	/**  `models-request` returns a live model list for this agent. */
	models?: boolean,
	/**  `usage-request` returns subscription usage for this agent's sessions. */
	usage?: boolean,
	/**  Sessions may be bound to a custom provider profile (`providerId`). */
	providers?: boolean,
	/**  `gsd-request` returns GSD workflow state for this agent's sessions. */
	gsd?: boolean,
	/**  `interrupt` stops the running turn. */
	interrupt?: boolean,
};

/**  Bridge → host. */
export type BridgeMessage = BridgeMessage_Serialize | BridgeMessage_Deserialize;

/**  Bridge → host. */
export type BridgeMessage_Deserialize = 
/**  First request after spawning the host. Reply: `initialized`. */
{ kind: "initialize"; payload: {
	bridgeVersion: string,
} } | 
/**
 *  Reply: `ack` once the agent is starting (its progress follows as
 *  session events), or `error` when it cannot start at all.
 */
{ kind: "start-session"; payload: StartSession_Deserialize } | 
/**  Stop the session and forget it. Reply: `ack`. No `ended` follows. */
{ kind: "end-session"; payload: {
	sessionId: string,
} } | 
/**  Hand user input to the agent. Reply: `ack`. */
{ kind: "prompt"; payload: {
	sessionId: string,
	text: string,
} } | 
/**  Stop the running turn. Reply: `ack`. */
{ kind: "interrupt"; payload: {
	sessionId: string,
} } | 
/**  Reply: `ack` when the agent applied it, else `error`. */
{ kind: "set-option"; payload: {
	sessionId: string,
	option: SessionOption,
	value: string,
} } | 
/**  Reply: `models`. */
{ kind: "list-models"; payload: {
	agent: string,
} } | 
/**  Reply: `usage`. */
{ kind: "get-usage"; payload: {
	sessionId: string,
} } | 
/**  Check a credential value with its provider. Reply: `credential-checked`. */
{ kind: "check-credential"; payload: {
	agent: string,
	credential: string,
	value: Secret,
} } | 
/**  Reply to `request-permission`. */
{ kind: "permission-outcome"; payload: SelectOutcome } | 
/**  Reply to `request-plan-approval`. */
{ kind: "plan-outcome"; payload: SelectOutcome } | 
/**  Reply to `ask-question`. */
{ kind: "question-outcome"; payload: QuestionOutcome } | 
/**  Reply to `call-host-tool`. */
{ kind: "host-tool-result"; payload: {
	text: string,
	isError?: boolean,
} };

/**  Bridge → host. */
export type BridgeMessage_Serialize = 
/**  First request after spawning the host. Reply: `initialized`. */
{ kind: "initialize"; payload: {
	bridgeVersion: string,
} } | 
/**
 *  Reply: `ack` once the agent is starting (its progress follows as
 *  session events), or `error` when it cannot start at all.
 */
{ kind: "start-session"; payload: StartSession_Serialize } | 
/**  Stop the session and forget it. Reply: `ack`. No `ended` follows. */
{ kind: "end-session"; payload: {
	sessionId: string,
} } | 
/**  Hand user input to the agent. Reply: `ack`. */
{ kind: "prompt"; payload: {
	sessionId: string,
	text: string,
} } | 
/**  Stop the running turn. Reply: `ack`. */
{ kind: "interrupt"; payload: {
	sessionId: string,
} } | 
/**  Reply: `ack` when the agent applied it, else `error`. */
{ kind: "set-option"; payload: {
	sessionId: string,
	option: SessionOption,
	value: string,
} } | 
/**  Reply: `models`. */
{ kind: "list-models"; payload: {
	agent: string,
} } | 
/**  Reply: `usage`. */
{ kind: "get-usage"; payload: {
	sessionId: string,
} } | 
/**  Check a credential value with its provider. Reply: `credential-checked`. */
{ kind: "check-credential"; payload: {
	agent: string,
	credential: string,
	value: Secret,
} } | 
/**  Reply to `request-permission`. */
{ kind: "permission-outcome"; payload: SelectOutcome } | 
/**  Reply to `request-plan-approval`. */
{ kind: "plan-outcome"; payload: SelectOutcome } | 
/**  Reply to `ask-question`. */
{ kind: "question-outcome"; payload: QuestionOutcome } | 
/**  Reply to `call-host-tool`. */
{ kind: "host-tool-result"; payload: {
	text: string,
	isError?: boolean,
} };

/**
 *  A credential an agent can use. The bridge stores the value and reports
 *  its status to the phone; the host decides how the agent consumes it.
 */
export type CredentialSpec = CredentialSpec_Serialize | CredentialSpec_Deserialize;

/**
 *  A credential an agent can use. The bridge stores the value and reports
 *  its status to the phone; the host decides how the agent consumes it.
 */
export type CredentialSpec_Deserialize = {
	id: string,
	label: string,
	/**
	 *  The environment variable that provides this credential when the
	 *  bridge's operator sets it themselves; such a value wins over a stored
	 *  one and the phone cannot clear it.
	 */
	envVar?: string | null,
};

/**
 *  A credential an agent can use. The bridge stores the value and reports
 *  its status to the phone; the host decides how the agent consumes it.
 */
export type CredentialSpec_Serialize = {
	id: string,
	label: string,
	/**
	 *  The environment variable that provides this credential when the
	 *  bridge's operator sets it themselves; such a value wins over a stored
	 *  one and the phone cannot clear it.
	 */
	envVar?: string | null,
};

export type DiffLine = {
	type: DiffLineType,
	text: string,
};

export type DiffLineType = "add" | "del" | "context";

/**  What a transcript entry is. Tagged by `entryType`. */
export type EntryBody = EntryBody_Serialize | EntryBody_Deserialize;

/**  What a transcript entry is. Tagged by `entryType`. */
export type EntryBody_Deserialize = 
/**
 *  Conversation text. Agent text written alongside tool calls may set
 *  `collapsible`, letting a client fold it into the tool group.
 */
({ entryType: "text"; role: Role; text: string; collapsible?: boolean }) & { callId?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  A plan the agent proposes (rendered as markdown, never collapsed). */
({ entryType: "plan"; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  Model reasoning. `redacted` = the provider withheld the content. */
({ entryType: "thinking"; text: string; redacted?: boolean }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | ({ entryType: "tool_call"; callId: string; 
/**  The agent's own tool name (display only — clients branch on `kind`). */
toolName: string; kind: ToolKind; 
/**  One-line human summary, e.g. `npm test` or `src/main.rs`. */
title: string; 
/**  Files or paths the call touches. */
locations?: string[]; 
/**  The agent's raw tool input, for detailed rendering. */
rawInput?: unknown | null }) & { collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; lines?: never; multiSelect?: never; options?: never; path?: never; question?: never; redacted?: never; requestId?: never; role?: never; summary?: never; text?: never; truncated?: never } | ({ entryType: "tool_result"; callId: string; text: string; isError?: boolean }) & { collapsible?: never; count?: never; description?: never; header?: never; index?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  A file change, as add/del/context lines. */
({ entryType: "diff"; path: string; lines: DiffLine[]; truncated?: boolean; callId?: string | null }) & { collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; locations?: never; multiSelect?: never; options?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never } | 
/**
 *  The agent is waiting for the user to allow or deny a tool call.
 *  Answered with `permission-response` using one of `options`.
 */
({ entryType: "permission_request"; requestId: string; toolName: string; kind: ToolKind; title: string; description?: string | null; locations?: string[]; rawInput?: unknown | null; options: PermissionOption[] }) & { callId?: never; collapsible?: never; count?: never; header?: never; index?: never; isError?: never; lines?: never; multiSelect?: never; path?: never; question?: never; redacted?: never; role?: never; summary?: never; text?: never; truncated?: never } | 
/**
 *  One question of a (possibly multi-question) ask, all sharing
 *  `request_id`. Answered with `question-response`.
 */
({ entryType: "question"; requestId: string; index: number; count: number; header?: string | null; question: string; options?: QuestionOption_Deserialize[]; multiSelect?: boolean }) & { callId?: never; collapsible?: never; description?: never; isError?: never; kind?: never; lines?: never; locations?: never; path?: never; rawInput?: never; redacted?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never; truncated?: never } | 
/**
 *  The agent finished planning and asks how to proceed. Answered with
 *  `plan-response` using one of `options`.
 */
({ entryType: "plan_approval"; requestId: string; options: OptionChoice_Deserialize[] }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; path?: never; question?: never; rawInput?: never; redacted?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never; truncated?: never } | 
/**
 *  A permission request, question or plan approval was answered (or
 *  cancelled); `summary` is a short human description of the outcome.
 */
({ entryType: "resolved"; requestId: string; summary: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; role?: never; text?: never; title?: never; toolName?: never; truncated?: never } | ({ entryType: "notice"; kind: NoticeKind; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  A one-line status message from the bridge or agent. */
({ entryType: "status"; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | ({ entryType: "error"; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  The agent's turn ended; it is waiting for input. */
({ entryType: "turn_complete" }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never; truncated?: never };

/**  What a transcript entry is. Tagged by `entryType`. */
export type EntryBody_Serialize = 
/**
 *  Conversation text. Agent text written alongside tool calls may set
 *  `collapsible`, letting a client fold it into the tool group.
 */
({ entryType: "text"; role: Role; text: string; collapsible?: boolean }) & { callId?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  A plan the agent proposes (rendered as markdown, never collapsed). */
({ entryType: "plan"; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  Model reasoning. `redacted` = the provider withheld the content. */
({ entryType: "thinking"; text: string; redacted?: boolean }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | ({ entryType: "tool_call"; callId: string; 
/**  The agent's own tool name (display only — clients branch on `kind`). */
toolName: string; kind: ToolKind; 
/**  One-line human summary, e.g. `npm test` or `src/main.rs`. */
title: string; 
/**  Files or paths the call touches. */
locations?: string[]; 
/**  The agent's raw tool input, for detailed rendering. */
rawInput?: unknown | null }) & { collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; lines?: never; multiSelect?: never; options?: never; path?: never; question?: never; redacted?: never; requestId?: never; role?: never; summary?: never; text?: never; truncated?: never } | ({ entryType: "tool_result"; callId: string; text: string; isError?: boolean }) & { collapsible?: never; count?: never; description?: never; header?: never; index?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  A file change, as add/del/context lines. */
({ entryType: "diff"; path: string; lines: DiffLine[]; truncated?: boolean; callId?: string | null }) & { collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; locations?: never; multiSelect?: never; options?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never } | 
/**
 *  The agent is waiting for the user to allow or deny a tool call.
 *  Answered with `permission-response` using one of `options`.
 */
({ entryType: "permission_request"; requestId: string; toolName: string; kind: ToolKind; title: string; description?: string | null; locations?: string[]; rawInput?: unknown | null; options: PermissionOption[] }) & { callId?: never; collapsible?: never; count?: never; header?: never; index?: never; isError?: never; lines?: never; multiSelect?: never; path?: never; question?: never; redacted?: never; role?: never; summary?: never; text?: never; truncated?: never } | 
/**
 *  One question of a (possibly multi-question) ask, all sharing
 *  `request_id`. Answered with `question-response`.
 */
({ entryType: "question"; requestId: string; index: number; count: number; header?: string | null; question: string; options: QuestionOption_Serialize[]; multiSelect?: boolean }) & { callId?: never; collapsible?: never; description?: never; isError?: never; kind?: never; lines?: never; locations?: never; path?: never; rawInput?: never; redacted?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never; truncated?: never } | 
/**
 *  The agent finished planning and asks how to proceed. Answered with
 *  `plan-response` using one of `options`.
 */
({ entryType: "plan_approval"; requestId: string; options: OptionChoice_Serialize[] }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; path?: never; question?: never; rawInput?: never; redacted?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never; truncated?: never } | 
/**
 *  A permission request, question or plan approval was answered (or
 *  cancelled); `summary` is a short human description of the outcome.
 */
({ entryType: "resolved"; requestId: string; summary: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; role?: never; text?: never; title?: never; toolName?: never; truncated?: never } | ({ entryType: "notice"; kind: NoticeKind; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  A one-line status message from the bridge or agent. */
({ entryType: "status"; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | ({ entryType: "error"; text: string }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; title?: never; toolName?: never; truncated?: never } | 
/**  The agent's turn ended; it is waiting for input. */
({ entryType: "turn_complete" }) & { callId?: never; collapsible?: never; count?: never; description?: never; header?: never; index?: never; isError?: never; kind?: never; lines?: never; locations?: never; multiSelect?: never; options?: never; path?: never; question?: never; rawInput?: never; redacted?: never; requestId?: never; role?: never; summary?: never; text?: never; title?: never; toolName?: never; truncated?: never };

/**
 *  One line on the pipe: `{ "v": 1, "id"?: string, "kind": …, "payload": … }`.
 *  Requests and their replies share `id`; notifications have none.
 */
export type Frame<M> = Frame_Serialize<M> | Frame_Deserialize<M>;

/**
 *  One line on the pipe: `{ "v": 1, "id"?: string, "kind": …, "payload": … }`.
 *  Requests and their replies share `id`; notifications have none.
 */
export type Frame_Deserialize<M> = {
	v: number,
	id?: string | null,
} & M;

/**
 *  One line on the pipe: `{ "v": 1, "id"?: string, "kind": …, "payload": … }`.
 *  Requests and their replies share `id`; notifications have none.
 */
export type Frame_Serialize<M> = {
	v: number,
	id?: string | null,
} & M;

/**  Host → bridge. */
export type HostMessage = HostMessage_Serialize | HostMessage_Deserialize;

/**  Host → bridge. */
export type HostMessage_Deserialize = 
/**  Reply to `initialize`. */
{ kind: "initialized"; payload: {
	hostVersion: string,
	agents: AgentInfo_Deserialize[],
} } | ({ kind: "ack" }) & { payload?: never } | 
/**  Reply to any request that failed. */
{ kind: "error"; payload: {
	message: string,
} } | 
/**  Reply to `list-models`. */
{ kind: "models"; payload: {
	models: ModelEntry_Deserialize[],
	defaultModel?: string | null,
} } | 
/**  Reply to `get-usage`; absent when the agent has none to report. */
{ kind: "usage"; payload: {
	usage?: UsageData_Deserialize | null,
} } | 
/**  Reply to `check-credential`; absent `valid` = it could not be checked. */
{ kind: "credential-checked"; payload: {
	valid?: boolean | null,
} } | 
/**  A notification: no frame id, no reply. */
{ kind: "session-event"; payload: {
	sessionId: string,
	event: SessionEvent_Deserialize,
} } | 
/**  Reply: `permission-outcome`. */
{ kind: "request-permission"; payload: PermissionRequest_Deserialize } | 
/**  Reply: `question-outcome`. */
{ kind: "ask-question"; payload: QuestionRequest_Deserialize } | 
/**  Reply: `plan-outcome`. */
{ kind: "request-plan-approval"; payload: PlanApprovalRequest_Deserialize } | 
/**  Reply: `host-tool-result`. */
{ kind: "call-host-tool"; payload: HostToolCall };

/**  Host → bridge. */
export type HostMessage_Serialize = 
/**  Reply to `initialize`. */
{ kind: "initialized"; payload: {
	hostVersion: string,
	agents: AgentInfo_Serialize[],
} } | ({ kind: "ack" }) & { payload?: never } | 
/**  Reply to any request that failed. */
{ kind: "error"; payload: {
	message: string,
} } | 
/**  Reply to `list-models`. */
{ kind: "models"; payload: {
	models: ModelEntry_Serialize[],
	defaultModel?: string | null,
} } | 
/**  Reply to `get-usage`; absent when the agent has none to report. */
{ kind: "usage"; payload: {
	usage?: UsageData_Serialize | null,
} } | 
/**  Reply to `check-credential`; absent `valid` = it could not be checked. */
{ kind: "credential-checked"; payload: {
	valid?: boolean | null,
} } | 
/**  A notification: no frame id, no reply. */
{ kind: "session-event"; payload: {
	sessionId: string,
	event: SessionEvent_Serialize,
} } | 
/**  Reply: `permission-outcome`. */
{ kind: "request-permission"; payload: PermissionRequest_Serialize } | 
/**  Reply: `question-outcome`. */
{ kind: "ask-question"; payload: QuestionRequest_Serialize } | 
/**  Reply: `plan-outcome`. */
{ kind: "request-plan-approval"; payload: PlanApprovalRequest_Serialize } | 
/**  Reply: `host-tool-result`. */
{ kind: "call-host-tool"; payload: HostToolCall };

export type HostToolCall = {
	sessionId: string,
	tool: string,
	args: unknown,
};

/**
 *  A tool the bridge implements and offers to a session's agent (the
 *  device-test tools). The driver exposes it to its agent under `name`;
 *  every call comes back to the bridge as `call-host-tool`.
 */
export type HostToolSpec = {
	name: string,
	description: string,
	/**
	 *  JSON Schema of the arguments: an object schema whose properties are
	 *  `string`, `number`, `integer` or `boolean`.
	 */
	inputSchema: unknown,
};

export type ModelEntry = ModelEntry_Serialize | ModelEntry_Deserialize;

export type ModelEntry_Deserialize = {
	id: string,
	label?: string | null,
};

export type ModelEntry_Serialize = {
	id: string,
	label?: string | null,
};

/**  Session lifecycle notices a client shows as a marker line. */
export type NoticeKind = 
/**  The agent process was restarted; the conversation may continue fresh. */
"session_restart" | 
/**  The agent process died unexpectedly. */
"session_died" | 
/**  The session could not start or continue. */
"session_failed" | 
/**  The agent rejected its credentials. */
"auth_error" | 
/**  A device screenshot was delivered (test sessions). */
"screenshot";

/**
 *  One selectable value of a per-agent option (a mode, an effort level, a
 *  plan-approval choice). `id` is what rides the wire; `label` is for display.
 */
export type OptionChoice = OptionChoice_Serialize | OptionChoice_Deserialize;

/**
 *  One selectable value of a per-agent option (a mode, an effort level, a
 *  plan-approval choice). `id` is what rides the wire; `label` is for display.
 */
export type OptionChoice_Deserialize = {
	id: string,
	label: string,
	description?: string | null,
};

/**
 *  One selectable value of a per-agent option (a mode, an effort level, a
 *  plan-approval choice). `id` is what rides the wire; `label` is for display.
 */
export type OptionChoice_Serialize = {
	id: string,
	label: string,
	description?: string | null,
};

/**  One transcript entry. */
export type OutputEntry = OutputEntry_Serialize | OutputEntry_Deserialize;

/**  One transcript entry. */
export type OutputEntry_Deserialize = {
	timestamp: string,
	/**  Set when a sub-agent (not the session's main agent) produced it. */
	subagent?: Subagent_Deserialize | null,
	/**
	 *  Agent-specific data no client depends on — the one sanctioned escape
	 *  hatch; anything a client renders belongs in a typed field instead.
	 */
	agentExtras?: unknown | null,
} & EntryBody_Deserialize;

/**  One transcript entry. */
export type OutputEntry_Serialize = {
	timestamp: string,
	/**  Set when a sub-agent (not the session's main agent) produced it. */
	subagent?: Subagent_Serialize | null,
	/**
	 *  Agent-specific data no client depends on — the one sanctioned escape
	 *  hatch; anything a client renders belongs in a typed field instead.
	 */
	agentExtras?: unknown | null,
} & EntryBody_Serialize;

export type PermissionOption = {
	id: string,
	label: string,
	kind: PermissionOptionKind,
};

/**
 *  What choosing a permission option does — lets a client style and order
 *  the choices without knowing the agent.
 */
export type PermissionOptionKind = "allow_once" | "allow_always" | "reject_once" | "reject_always";

export type PermissionRequest = PermissionRequest_Serialize | PermissionRequest_Deserialize;

export type PermissionRequest_Deserialize = {
	sessionId: string,
	/**  Identifies the card on the phone (and in `resolved`). */
	requestId: string,
	toolName: string,
	kind: ToolKind,
	title: string,
	description?: string | null,
	locations?: string[],
	rawInput?: unknown | null,
	options: PermissionOption[],
	subagent?: Subagent_Deserialize | null,
};

export type PermissionRequest_Serialize = {
	sessionId: string,
	/**  Identifies the card on the phone (and in `resolved`). */
	requestId: string,
	toolName: string,
	kind: ToolKind,
	title: string,
	description?: string | null,
	locations?: string[],
	rawInput?: unknown | null,
	options: PermissionOption[],
	subagent?: Subagent_Serialize | null,
};

export type PlanApprovalRequest = PlanApprovalRequest_Serialize | PlanApprovalRequest_Deserialize;

export type PlanApprovalRequest_Deserialize = {
	sessionId: string,
	requestId: string,
	options: OptionChoice_Deserialize[],
};

export type PlanApprovalRequest_Serialize = {
	sessionId: string,
	requestId: string,
	options: OptionChoice_Serialize[],
};

/**  A custom provider profile a session is bound to for its whole life. */
export type ProviderBinding = ProviderBinding_Serialize | ProviderBinding_Deserialize;

/**  A custom provider profile a session is bound to for its whole life. */
export type ProviderBinding_Deserialize = {
	id: string,
	baseUrl: string,
	authToken: Secret,
	models: ProviderModel_Deserialize[],
	defaultModel?: string | null,
};

/**  A custom provider profile a session is bound to for its whole life. */
export type ProviderBinding_Serialize = {
	id: string,
	baseUrl: string,
	authToken: Secret,
	models: ProviderModel_Serialize[],
	defaultModel?: string | null,
};

export type ProviderModel = ProviderModel_Serialize | ProviderModel_Deserialize;

export type ProviderModel_Deserialize = {
	id: string,
	label?: string | null,
};

export type ProviderModel_Serialize = {
	id: string,
	label?: string | null,
};

export type QuestionOption = QuestionOption_Serialize | QuestionOption_Deserialize;

export type QuestionOption_Deserialize = {
	label: string,
	description?: string | null,
};

export type QuestionOption_Serialize = {
	label: string,
	description?: string | null,
};

export type QuestionOutcome = 
/**
 *  One answer per question, in order; a chosen option is its label
 *  (several joined with ", "), free text is the text.
 */
{ outcome: "answered"; answers: string[] } | { outcome: "cancelled"; reason: string };

export type QuestionRequest = QuestionRequest_Serialize | QuestionRequest_Deserialize;

export type QuestionRequest_Deserialize = {
	sessionId: string,
	requestId: string,
	questions: QuestionSpec_Deserialize[],
};

export type QuestionRequest_Serialize = {
	sessionId: string,
	requestId: string,
	questions: QuestionSpec_Serialize[],
};

export type QuestionSpec = QuestionSpec_Serialize | QuestionSpec_Deserialize;

export type QuestionSpec_Deserialize = {
	header?: string | null,
	question: string,
	options?: QuestionOption_Deserialize[],
	multiSelect?: boolean,
};

export type QuestionSpec_Serialize = {
	header?: string | null,
	question: string,
	options: QuestionOption_Serialize[],
	multiSelect?: boolean,
};

/**  Who wrote a text entry. */
export type Role = "user" | "agent";

/**
 *  A credential value (API key, token) crossing the bridge/host pipe.
 * 
 *  Serializes as a plain string — the pipe is a local process boundary, and
 *  the host needs the value to hand it to the agent — but its `Debug` output
 *  is redacted, so a message containing one can be logged with `{:?}`
 *  without leaking the secret.
 */
export type Secret = string;

/**  The answer to a permission request or a plan approval. */
export type SelectOutcome = 
/**  One of the request's options. */
{ outcome: "selected"; optionId: string } | 
/**  Nobody chose: timed out, interrupted, the session is ending. */
{ outcome: "cancelled"; reason: string };

/**
 *  Something a running session reports. The host translates its agent's own
 *  events into these; the bridge never sees an SDK message.
 */
export type SessionEvent = SessionEvent_Serialize | SessionEvent_Deserialize;

/**
 *  Something a running session reports. The host translates its agent's own
 *  events into these; the bridge never sees an SDK message.
 */
export type SessionEvent_Deserialize = 
/**  The agent came up and accepts prompts. Sent once per `start-session`. */
({ type: "ready" }) & { contextPercentage?: never; contextWindow?: never; entries?: never; error?: never; mode?: never; model?: never; nativeSessionId?: never; resumeLost?: never; state?: never } | 
/**  Session facts changed. Only the fields that changed are set. */
({ type: "info"; 
/**  The agent's own conversation id — the `resume` target. */
nativeSessionId?: string | null; 
/**  The model the agent actually resolved. */
model?: string | null; 
/**
 *  The agent changed its own mode (e.g. it entered plan mode, or a
 *  plan approval switched it).
 */
mode?: string | null; contextWindow?: number | null; 
/**  0–100. */
contextPercentage?: number | null }) & { entries?: never; error?: never; resumeLost?: never; state?: never } | 
/**  Transcript entries, in order. The bridge assigns their seqs. */
({ type: "entries"; entries: OutputEntry_Deserialize[] }) & { contextPercentage?: never; contextWindow?: never; error?: never; mode?: never; model?: never; nativeSessionId?: never; resumeLost?: never; state?: never } | ({ type: "turn"; state: TurnState }) & { contextPercentage?: never; contextWindow?: never; entries?: never; error?: never; mode?: never; model?: never; nativeSessionId?: never; resumeLost?: never } | 
/**
 *  The session is gone. Absent `error` = it ended normally. The host
 *  forgets the session after sending this.
 */
({ type: "ended"; error?: string | null; 
/**
 *  The agent could not find the `resume` conversation, so continuing
 *  it is impossible; a new start must be fresh.
 */
resumeLost?: boolean }) & { contextPercentage?: never; contextWindow?: never; entries?: never; mode?: never; model?: never; nativeSessionId?: never; state?: never };

/**
 *  Something a running session reports. The host translates its agent's own
 *  events into these; the bridge never sees an SDK message.
 */
export type SessionEvent_Serialize = 
/**  The agent came up and accepts prompts. Sent once per `start-session`. */
({ type: "ready" }) & { contextPercentage?: never; contextWindow?: never; entries?: never; error?: never; mode?: never; model?: never; nativeSessionId?: never; resumeLost?: never; state?: never } | 
/**  Session facts changed. Only the fields that changed are set. */
({ type: "info"; 
/**  The agent's own conversation id — the `resume` target. */
nativeSessionId?: string | null; 
/**  The model the agent actually resolved. */
model?: string | null; 
/**
 *  The agent changed its own mode (e.g. it entered plan mode, or a
 *  plan approval switched it).
 */
mode?: string | null; contextWindow?: number | null; 
/**  0–100. */
contextPercentage?: number | null }) & { entries?: never; error?: never; resumeLost?: never; state?: never } | 
/**  Transcript entries, in order. The bridge assigns their seqs. */
({ type: "entries"; entries: OutputEntry_Serialize[] }) & { contextPercentage?: never; contextWindow?: never; error?: never; mode?: never; model?: never; nativeSessionId?: never; resumeLost?: never; state?: never } | ({ type: "turn"; state: TurnState }) & { contextPercentage?: never; contextWindow?: never; entries?: never; error?: never; mode?: never; model?: never; nativeSessionId?: never; resumeLost?: never } | 
/**
 *  The session is gone. Absent `error` = it ended normally. The host
 *  forgets the session after sending this.
 */
({ type: "ended"; error?: string | null; 
/**
 *  The agent could not find the `resume` conversation, so continuing
 *  it is impossible; a new start must be fresh.
 */
resumeLost?: boolean }) & { contextPercentage?: never; contextWindow?: never; entries?: never; mode?: never; model?: never; nativeSessionId?: never; state?: never };

/**
 *  The per-session options `set-option` changes and `option-confirmed`
 *  reports. Values are agent-defined strings (see [`AgentDescriptor`]).
 */
export type SessionOption = "mode" | "effort" | "model";

export type StartSession = StartSession_Serialize | StartSession_Deserialize;

export type StartSession_Deserialize = {
	/**
	 *  The bridge's session id — every later message about this session
	 *  carries it.
	 */
	sessionId: string,
	agent: string,
	cwd: string,
	mode?: string | null,
	effort?: string | null,
	model?: string | null,
	/**
	 *  The agent's own conversation id to continue (from an earlier
	 *  `info.nativeSessionId`); absent = start a fresh conversation.
	 */
	resume?: string | null,
	/**  Stored values of this agent's credentials, by [`CredentialSpec`] id. */
	credentials?: { [key in string]: Secret },
	/**
	 *  Extra environment for the agent's processes (bridge-level credentials
	 *  such as `GITHUB_TOKEN`).
	 */
	env?: { [key in string]: Secret },
	provider?: ProviderBinding_Deserialize | null,
	hostTools?: HostToolSpec[],
	/**
	 *  Refuse every tool call that touches signing keystores or secret files,
	 *  whatever the mode (device-test sessions).
	 */
	denySecretPaths?: boolean,
};

export type StartSession_Serialize = {
	/**
	 *  The bridge's session id — every later message about this session
	 *  carries it.
	 */
	sessionId: string,
	agent: string,
	cwd: string,
	mode?: string | null,
	effort?: string | null,
	model?: string | null,
	/**
	 *  The agent's own conversation id to continue (from an earlier
	 *  `info.nativeSessionId`); absent = start a fresh conversation.
	 */
	resume?: string | null,
	/**  Stored values of this agent's credentials, by [`CredentialSpec`] id. */
	credentials?: { [key in string]: Secret },
	/**
	 *  Extra environment for the agent's processes (bridge-level credentials
	 *  such as `GITHUB_TOKEN`).
	 */
	env?: { [key in string]: Secret },
	provider?: ProviderBinding_Serialize | null,
	hostTools?: HostToolSpec[],
	/**
	 *  Refuse every tool call that touches signing keystores or secret files,
	 *  whatever the mode (device-test sessions).
	 */
	denySecretPaths?: boolean,
};

/**  Identifies the sub-agent that produced an entry. */
export type Subagent = Subagent_Serialize | Subagent_Deserialize;

/**  Identifies the sub-agent that produced an entry. */
export type Subagent_Deserialize = {
	label?: string | null,
};

/**  Identifies the sub-agent that produced an entry. */
export type Subagent_Serialize = {
	label?: string | null,
};

/**
 *  What a tool call does, normalized across agents (the Agent Client
 *  Protocol's tool kinds). Clients pick icons and summaries from this rather
 *  than from agent-specific tool names.
 */
export type ToolKind = "read" | "edit" | "delete" | "move" | "search" | "execute" | "think" | "fetch" | "switch_mode" | "other";

export type TurnState = "running" | "idle";

export type UsageData = UsageData_Serialize | UsageData_Deserialize;

export type UsageData_Deserialize = {
	available: boolean,
	/**  Subscription / plan name, when the agent's provider reports one. */
	plan?: string | null,
	windows?: UsageWindow[],
	sessionCostUsd?: number | null,
	fetchedAt: string,
};

export type UsageData_Serialize = {
	available: boolean,
	/**  Subscription / plan name, when the agent's provider reports one. */
	plan?: string | null,
	windows: UsageWindow[],
	sessionCostUsd?: number | null,
	fetchedAt: string,
};

export type UsageWindow = {
	/**  e.g. "5h", "7d", "7d Opus". */
	label: string,
	/**  0–100. */
	utilization: number | null,
	resetsAt: string | null,
};
